from setuptools import find_packages, setup
setup(
    name = "src", #Source
    version = "0.0.1",
    author = "Priyanshu_Dhingra",
    author_email = "priyanshudhingra1@gmail.com",
    packages = find_packages(), #Whatever written inside requirements.txt will go inside this
    install_requires = [] # For installing packages mentioned inside requirements.txr
)